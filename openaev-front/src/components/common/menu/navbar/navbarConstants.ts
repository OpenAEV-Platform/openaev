/** Rail widths, mirroring the library Navbar's `w-45` / `w-12` — see fds-migration/LIBRARY-FEEDBACK.md #20 */
export const NAV_OPEN_WIDTH = 180;
export const NAV_COLLAPSED_WIDTH = 48;

/** The rail's own width transition, measured in the browser — see fds-migration/LIBRARY-FEEDBACK.md #20 */
export const NAV_WIDTH_TRANSITION = 'width 0.15s cubic-bezier(0.4, 0, 0.2, 1)';
