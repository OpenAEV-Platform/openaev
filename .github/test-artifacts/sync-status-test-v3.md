# Disposable test file

This file only exists to create a mergeable diff for testing the
`sync-status-on-merge` action (retry #3, after fixing the ProjectV2Owner
GraphQL field resolution). Safe to delete after the test.

Related issue: #6619
